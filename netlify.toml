[build]
  publish = "public"
  command = ""

# Static site — no build step. Only public/ is served, so .maestro/ stays private.
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
